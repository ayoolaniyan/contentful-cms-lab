{
  "name": "contentful-cms-lab",
  "private": true,
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:psql": "docker compose exec postgres psql -U cms -d cms_lab",
    "sync": "pnpm --filter sync-service dev",
    "web": "pnpm --filter web dev"
  }
}
